# ─── ClearBag バックエンド ローカル開発環境変数 ─────────────────────────────────
# このファイルをコピーして .env.local を作成してください。
# cp .env.local.example .env.local

# ── ローカル開発モード ──────────────────────────────────────────────────────────
# true にすると Cloud Tasks をスキップし、BackgroundTasks で分析を実行する
LOCAL_MODE=true

# ── 開発用フラグ ──────────────────────────────────────────────────────────────
# true にすると無料プランの月間解析枚数制限をスキップ
DISABLE_RATE_LIMIT=true

# ログイン許可メールアドレス（カンマ区切り）。未設定の場合は全員許可。
# 例: ALLOWED_EMAILS=you@example.com,colleague@example.com
# ALLOWED_EMAILS=

# ── GCP プロジェクト ──────────────────────────────────────────────────────────
# Vertex AI Gemini の呼び出しに使用（ADC 認証: gcloud auth application-default login）
PROJECT_ID=your-gcp-project-id

# ── Firebase プロジェクト ─────────────────────────────────────────────────────
# Firebase Auth トークンの検証に使用するプロジェクト ID
# GCP プロジェクト ID と異なる場合のみ設定（同じなら PROJECT_ID が使われる）
FIREBASE_PROJECT_ID=your-firebase-project-id

# ── Firestore エミュレーター ───────────────────────────────────────────────────
# docker compose up -d で起動したエミュレーターに接続
FIRESTORE_EMULATOR_HOST=localhost:8089

# ── GCS エミュレーター (fake-gcs-server) ─────────────────────────────────────
STORAGE_EMULATOR_HOST=http://localhost:4443
GCS_BUCKET_NAME=clearbag-local

# ── Vertex AI Gemini ──────────────────────────────────────────────────────────
VERTEX_AI_LOCATION=asia-northeast1
GEMINI_MODEL=gemini-2.5-pro

# ── 以下は LOCAL_MODE=true では使用しない（記述不要）────────────────────────────
# CLOUD_TASKS_QUEUE=
# WORKER_URL=
# SERVICE_ACCOUNT_EMAIL=
# SENDGRID_API_KEY=
# VAPID_PRIVATE_KEY=
# VAPID_PUBLIC_KEY=
# VAPID_CLAIMS_EMAIL=
